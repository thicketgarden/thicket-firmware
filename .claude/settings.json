{
  "permissions": {
    "deny": [
      "mcp__claude_ai_Gmail",
      "mcp__claude_ai_Google_Calendar",
      "mcp__claude_ai_Google_Drive",
      "mcp__plugin_figma_figma",
      "mcp__sentry",
      "mcp__vercel"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 's=\"${CLAUDE_PROJECT_DIR:-}/scripts/hooks/post-write-build.sh\"; [ -x \"$s\" ] || s=\"$PWD/scripts/hooks/post-write-build.sh\"; if [ -x \"$s\" ]; then exec \"$s\"; fi; echo \"build gate: cannot locate scripts/hooks/post-write-build.sh (CLAUDE_PROJECT_DIR=${CLAUDE_PROJECT_DIR:-<unset>}, PWD=$PWD). The gate did NOT run.\" >&2; exit 2'"
          }
        ]
      }
    ]
  }
}
